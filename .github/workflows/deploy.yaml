name: remote ssh command for deploy
on:
  push:
    branches: "main"
jobs:
  deploy:
    name: build
    runs-on: ubuntu-latest

    steps:
      - name: run delploy.sh
        run: |
          chmod +x ../../deploy.sh
          ../../deploy.sh
